import { render,screen } from "@testing-library/react";import { describe,expect,it } from "vitest";import { AlertBanner,MetricCard,ProgressRing } from ".";
describe("shared UI",()=>{it("renders operational metrics",()=>{render(<MetricCard label="Yield" value="4.2 t" detail="Season"/>);expect(screen.getByText("4.2 t")).toBeVisible()});it("labels progress rings",()=>{render(<ProgressRing value={82} label="ready"/>);expect(screen.getByRole("img")).toHaveAccessibleName("ready: 82%")});it("announces alerts",()=>{render(<AlertBanner title="Attention">Review now</AlertBanner>);expect(screen.getByRole("status")).toHaveTextContent("Review now")})});
