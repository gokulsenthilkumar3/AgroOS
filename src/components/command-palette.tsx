"use client";import { useEffect,useState } from "react";import { useRouter } from "next/navigation";import { Search,X } from "lucide-react";
const commands=[['Overview','/dashboard'],['All modules','/dashboard/modules'],['Crop lifecycle','/dashboard/crops'],['Analytics','/dashboard/analytics'],['Marketplace','/dashboard/marketplace'],['Cold chain','/dashboard/cold-chain'],['Administration','/dashboard/admin'],['Weather','/dashboard/weather'],['News','/dashboard/news']];
export function CommandPalette(){const[open,setOpen]=useState(false),[query,setQuery]=useState("");const router=useRouter();useEffect(()=>{const handler=(e:KeyboardEvent)=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();setOpen(v=>!v)}if(e.key==='Escape')setOpen(false)};addEventListener('keydown',handler);return()=>removeEventListener('keydown',handler)},[]);if(!open)return null;const matches=commands.filter(([name])=>name.toLowerCase().includes(query.toLowerCase()));return <div className="command-backdrop" onMouseDown={()=>setOpen(false)}><div className="command-palette" role="dialog" aria-label="Command palette" onMouseDown={e=>e.stopPropagation()}><label><Search size={17}/><input autoFocus value={query} onChange={e=>setQuery(e.target.value)} placeholder="Jump to a workspace…"/><button aria-label="Close" onClick={()=>setOpen(false)}><X size={16}/></button></label>{matches.map(([name,href])=><button key={href} onClick={()=>{setOpen(false);router.push(href)}}><span>{name}</span><kbd>↵</kbd></button>)}</div></div>}
