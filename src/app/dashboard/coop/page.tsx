import { coopRequests } from "@/lib/data";
export default function CoopPage() { return <><header className="page-header"><div><p className="eyebrow">AGRI-COOP</p><h1>Cooperative purchasing</h1><p className="muted">Aggregate demand, reduce input costs, and coordinate member allocations.</p></div><button className="button primary">Create request</button></header><section className="metric-grid"><article className="metric"><span>Active members</span><strong>38</strong><small>7 joined this month</small></article><article className="metric"><span>Open requests</span><strong>2</strong><small>Closes this week</small></article><article className="metric"><span>Estimated savings</span><strong>₹18,400</strong><small>Across open requests</small></article><article className="metric"><span>Soil reports</span><strong>12</strong><small>6 require review</small></article></section><section className="crop-grid full">{coopRequests.map(request => <article className="panel crop-card large" key={request.item}><p className="eyebrow">CLOSES {request.closes.toUpperCase()}</p><h2>{request.item}</h2><p>{request.supplier} · {request.members} members · target {request.target}</p><div className="progress"><i style={{ width: `${request.committed}%` }}/></div><div className="crop-meta"><span><strong>{request.committed}% committed</strong></span><span className="healthy-text">View allocation</span></div></article>)}</section></>; }
