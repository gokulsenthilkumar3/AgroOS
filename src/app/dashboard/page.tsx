import { AlertList } from "@/components/alert-list";
import { alerts, cropBatches, devices, farm, telemetry } from "@/lib/data";
function Metric({ label, value, detail, tone = "" }: { label: string; value: string; detail: string; tone?: string }) { return <article className={`metric ${tone}`}><span>{label}</span><strong>{value}</strong><small>{detail}</small></article>; }
export default function DashboardPage() { const max = Math.max(...telemetry.map(t => t.temperature)); const min = Math.min(...telemetry.map(t => t.temperature)); const points = telemetry.map((t, i) => `${i * 4.35},${90 - ((t.temperature - min) / (max - min)) * 68}`).join(" "); return <><header className="page-header"><div><p className="eyebrow">{farm.location.toUpperCase()}</p><h1>Good morning, Anika.</h1><p className="muted">Here’s how {farm.name} is doing today.</p></div><button className="button secondary">Export report</button></header><section className="metric-grid"><Metric label="Growing zones" value="3" detail="All zones reporting"/><Metric label="Connected devices" value="2 / 3" detail="1 device needs attention" tone="warning"/><Metric label="Active alerts" value="2" detail="1 critical condition" tone="critical"/><Metric label="Next harvest" value="4 days" detail="Butterhead lettuce"/></section><section className="dashboard-grid"><article className="panel chart-panel"><div className="section-heading"><div><p className="eyebrow">LAST 24 HOURS</p><h2>Climate trend</h2></div><span className="legend"><i/> Temperature °C</span></div><div className="chart" role="img" aria-label="Temperature changed between 21 and 25 degrees Celsius over the last 24 hours"><svg viewBox="0 0 100 100" preserveAspectRatio="none"><polyline points={points} /></svg><div className="chart-labels"><span>00:00</span><span>06:00</span><span>12:00</span><span>18:00</span><span>Now</span></div></div></article><article className="panel device-panel"><div className="section-heading"><div><p className="eyebrow">LIVE STATUS</p><h2>Device health</h2></div></div>{devices.map(d => <div className="device-row" key={d.id}><span className={`status-dot ${d.status === "Online" ? "healthy" : "warning"}`}/><div><strong>{d.name}</strong><small>{d.zone}</small></div><span>{d.status}</span></div>)}</article></section><section className="panel"><div className="section-heading"><div><p className="eyebrow">CURRENT CYCLES</p><h2>Crop batches</h2></div><a href="/dashboard/crops">View all</a></div><div className="crop-grid">{cropBatches.map(c => <article className="crop-card" key={c.crop}><div><strong>{c.crop}</strong><span>{c.zone}</span></div><div className="progress"><i style={{ width: `${c.progress}%` }}/></div><small>{c.progress}% complete · harvest {c.harvest}</small><b className={c.status === "On track" ? "healthy-text" : "warning-text"}>{c.status}</b></article>)}</div></section><AlertList initialAlerts={alerts}/></>; }
