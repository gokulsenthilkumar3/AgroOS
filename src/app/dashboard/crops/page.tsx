import { cropBatches } from "@/lib/data";
export default function CropsPage() { return <><header className="page-header"><div><p className="eyebrow">FARM OPERATIONS</p><h1>Crop batches</h1><p className="muted">Track each growing cycle, expected harvest, and operating condition.</p></div></header><section className="crop-grid full">{cropBatches.map(c => <article className="panel crop-card large" key={c.crop}><p className="eyebrow">{c.zone}</p><h2>{c.crop}</h2><div className="progress"><i style={{ width: `${c.progress}%` }}/></div><p>{c.progress}% through this cycle</p><div className="crop-meta"><span>Expected harvest <strong>{c.harvest}</strong></span><span className={c.status === "On track" ? "healthy-text" : "warning-text"}>{c.status}</span></div></article>)}</section></>; }
