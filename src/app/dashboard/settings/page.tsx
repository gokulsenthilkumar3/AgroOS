import { requireSession } from "@/lib/auth";
import { permissionsFor, type Role } from "@/lib/roles";
export default async function SettingsPage() { const user = await requireSession(); const permissions = permissionsFor(user.role as Role); return <><header className="page-header"><div><p className="eyebrow">SETTINGS</p><h1>Profile & workspace</h1><p className="muted">Personal preferences, organisation controls, and access visibility.</p></div></header><section className="settings-grid"><article className="panel settings-card"><p className="eyebrow">YOUR PROFILE</p><div className="profile-hero"><span className="profile-avatar">AS</span><div><h2>{user.name}</h2><p>{user.email}</p><span className="pill">Farm manager</span></div></div><label>Display name<input defaultValue={user.name}/></label><label>Email address<input defaultValue={user.email} type="email"/></label><button className="button primary">Save profile</button></article><article className="panel settings-card"><p className="eyebrow">ORGANISATION</p><h2>Green Valley Collective</h2><p className="muted">Pune, Maharashtra · India</p><div className="settings-row"><span>Default workspace</span><strong>HydroGrow</strong></div><div className="settings-row"><span>Plan</span><strong>Farm operations</strong></div><button className="button secondary">Manage organisation</button></article><article className="panel settings-card"><p className="eyebrow">ROLE & ACCESS</p><h2>What you can do</h2><p className="muted">Your access is evaluated on every server request.</p><ul className="permission-list">{permissions.map(item => <li key={item}>✓ {item.replace(":", " · ")}</li>)}</ul><button className="button secondary">View team roles</button></article><article className="panel settings-card"><p className="eyebrow">NOTIFICATIONS</p><h2>Farm signals</h2><div className="settings-row"><span>Critical alerts</span><strong>Push + email</strong></div><div className="settings-row"><span>Daily farm brief</span><strong>08:00 IST</strong></div><button className="button secondary">Configure notifications</button></article></section></>; }
