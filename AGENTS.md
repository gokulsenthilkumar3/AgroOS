# AgroOS contributor guidance

## Testing

Run `pnpm test:all` before handing off application changes. See `TESTING.md` for conventions. Add tests for new functions, both sides of conditionals, error handling, and fixed regressions. Existing tests must remain green.
